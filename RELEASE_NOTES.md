# WinLauncher 更新记录

本文档用于记录 WinLauncher 每个版本的变化，内容面向用户和发布说明，不记录具体代码实现细节。

维护规则：

- 每次修改、修复、新增、体验调整、文档更新或发布准备，都必须同步更新本文档。
- 按版本分节记录，最新版本放在最上方。
- 同一个版本内不要重复记录相同内容；如果多个修改属于同一体验或同一能力，应合并成一条更清晰的发布说明。
- 优先使用用户能理解的介绍语言，例如“改进启动体验”“修复弹窗显示异常”“新增配置备份能力”，避免使用文件名、函数名、提交号等实现细节。
- 如果当前修改不改变产品功能，也要在对应版本的“文档与维护”或“稳定性与维护”中用简短语言记录。
- Codex、Antigravity、OpenCode、WorkBuddy 以及其他 AI 智能体在完成任何项目修改前，都必须检查并维护本文档。

## v0.5.1.7

### 功能新增

- 新增原生 DLL 插件系统基础能力，支持扫描用户插件目录、校验插件清单、安装和卸载插件、启用/禁用插件、查看权限摘要、记录插件错误，并在设置面板中查看插件状态。
- 新增受信任插件命令闭环，已启用插件可注册普通命令和 / 命令，并在弹窗搜索中展示和执行；Host API 支持按权限访问版本信息、日志、剪贴板、打开链接/文件和插件私有文件读写。
- 新增插件动态搜索源和独立 / 命令搜索体验，插件可在后台返回普通搜索结果，内置和插件 / 命令可带图标并在未设置图标时使用默认图标，普通图标搜索与 / 命令保持独立匹配。
- 新增插件声明式配置基础能力，支持读取 manifest 中的配置项，在设置页编辑插件配置，并通过 Host API 按权限读写插件私有配置文件，不写入主程序配置。
- 扩展插件 Host API，新增输入框、密码框、选项选择、确认提示、文件选择、通知提示、加载/进度反馈、结果回显、网络请求、进程执行和屏幕信息等能力，便于插件完成更完整的交互流程。
- 系统信息插件新增 `/screen` 命令，可通过 Host API 显示主屏分辨率、DPI 和当前应用主题。
- 系统信息插件移除 `/screen` 命令，不再作为独立 slash command 暴露；屏幕信息仍可通过 `/sysinfo` 综合列表中查看。
- 命令输出面板底部按钮改为四个等宽铺满窗口（刷新/复制/确定/更多），更多按钮可通过下拉菜单展示内置快捷操作和插件注册的扩展功能；新增 `RegisterBuiltinPopupAction` API 供内置命令注册更多菜单入口。

### 修复

- 修复中键弹窗 `/` 搜索结果可能同时显示命令名和用法文本，导致看起来有两个图标名称的问题。
- 修复插件 / 命令在弹出输入框时可能因插件管理锁重入而卡死或触发未处理异常的问题，例如网络工具 `/port` 缺参数时的输入提示。
- 修复网络工具 `/dns` 在输入参数后搜索结果可能消失或点击无效的问题，并补齐网络插件的 Winsock 初始化流程。

### 体验优化

- 统一弹窗出现和消失动画为轻量淡入淡出，减少中键弹窗和其他弹窗开合时的边框缩放重影，并支持通过设置统一调节动画时长或关闭动画。
- 颜色工具插件新增自绘 HSV 取色器：`/colorconv` 不带参数时弹出可视化取色窗口（色相-饱和度平面 + 亮度滑条 + 实时预览），鼠标拖拽选色后自动转换输出 HEX / RGB / HSL 格式并复制 HEX 到剪贴板。
- 文本、文件、时间和网络工具插件完善缺参数时的交互体验：文本类命令可弹输入框，文件命令可直接使用 WinLauncher 已捕获的资源管理器/桌面选中文件，倒计时和网络诊断命令可直接提示补全参数；所有内置插件已重新编译并重新生成安装包。
- 文件工具插件移除 `/copypath` 命令，仅保留 `/fileinfo` 和 `/filehash`，其中 `/filehash` 支持对选中的文件夹生成递归目录哈希。
- 插件管理页支持将 `.wlplugin` 插件包直接拖入页面安装，多个插件包可连续处理，并会提示安装结果。
- 命令输出面板底部新增刷新按钮，可保留上次执行时的参数和选中文件列表重新执行命令。
- 优化命令面板刷新行为：刷新时清除旧输出内容，仅显示重新执行产生的新输出，不再拼接历史结果；修复长文本行在输出面板中被横向裁剪导致内容显示不完整的问题（如 SHA-256 哈希值尾部字符不可见）。

### 文档与维护

- 新增面向发布说明的版本更新记录，后续功能变化、修复和维护调整都将按版本集中记录。
- 明确智能体协作规则，要求 Codex、Antigravity/antygravity 等 AI 智能体在每次修改项目时同步维护更新记录，并避免同版本内容重复。
- 完善插件系统优化计划，明确插件能力边界、运行时目录、安全策略、阶段验收和后续 SDK/隔离进程路线。
- 新增 / 命令（Slash Commands）系统设计，包括概念定义、内置命令规划、插件注册方式、搜索匹配逻辑和执行路径，与普通搜索保持两条独立匹配路径。
- 新增插件开发 ABI 头文件、开发说明、Visual Studio 插件模板和可打包的 Hello World 示例插件，便于后续编译 DLL 并打包为插件包后安装验证。
- 优化插件权限摘要展示，设置页使用更容易理解的权限说明，便于安装或排查插件时确认它声明了哪些能力。
- 完善插件 API 接口计划书到 v1.2，明确基础接口和输入交互、文件选择、通知、加载/进度、结果回显、网络请求、进程执行等扩展接口均通过 `WLHostApiV1` 提供，并同步 SDK 封装和内置插件对接状态。
- 新增 `WLPopupActionDescriptorV1` 结构体和 `registerPopupAction` Host API 入口，允许插件在 `onLoad` 时注册命令面板下方更多菜单中的自定义操作项，并通过 `PluginManager::GetPopupActions()` 聚合展示。
- 新增插件开发者 README 和内置插件维护说明，集中说明可用接口、权限模型、开发规范、manifest 要求、构建打包流程和发布前检查项。
