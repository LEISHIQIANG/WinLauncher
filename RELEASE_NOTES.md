# WinLauncher 更新记录

本文档面向用户和发布说明，只记录产品功能变化（新增、修复、体验优化），不记录项目文档、开发计划等内部维护内容。

维护规则：

- 每次功能修改、修复或体验调整都必须同步更新本文档。
- 按版本分节记录，最新版本放在最上方。
- 同一个版本内不要出现自相矛盾的内容（如新增又删除同一功能），应直接合并为最终状态。
- 同版本内多个相关修改应合并为一条清晰的发布说明。
- 优先使用用户能理解的介绍语言，避免文件名、函数名、提交号等实现细节。
- Codex、Antigravity、OpenCode、WorkBuddy 以及其他 AI 智能体在完成任何项目修改前，都必须检查并维护本文档。

## v0.5.1.9

### 功能新增

- 命令类型菜单仅显示本机已检测到的 Python 版本和 Git Bash，选定解释器固定使用；通用 Python 命令保持兼容，缺失版本会提示重新选择。
- 所有内置预设均配有默认图标：系统工具与命令自动显示对应图标，网站预设采用各官网图标；自定义图标清空后自动恢复默认。移除了重复旧帧以减小体积。
- 内置图标的“系统”分类新增磁盘管理、服务、事件查看器、资源监视器和系统信息，可一键添加。
- 新增诊断与迁移工具：可从配置管理生成脱敏诊断 ZIP，或导出/导入含配置、历史、插件状态与私有数据的迁移备份；操作弹出系统“另存为”对话框，默认桌面并预填时间戳文件名，不携带插件程序或日志。
- 搜索结果优先按名称匹配质量，再结合本机使用次数和最近使用时间排序；记录仅限本机且可随时清除。
- 弹窗外观新增“图标字号”（8–32 px）和“标题大小”（1–9 档，默认 5）调节项；外观参数重新整理归类。
- 完善“关于软件”页面：展示功能简介、版本和运行方式；“插件管理”移至“配置管理”之前。

### 修复

- 修复托盘右键"重启应用"只会关闭程序而无法重启的问题：旧进程销毁主窗口后才启动新实例，避免新进程因 FindWindow 检测到旧窗口而误判为重复实例并退出。
- 修复托盘右键菜单弹出时光标短暂转圈的问题：托盘右键通知先立即返回系统，菜单在下一帧开始创建和渲染，且出现位置固定于托盘图标相对位置（不随光标位置漂移），消除光标繁忙状态的同时优化了交互体验。
- 修复网络工具 DNS 泄露检测在立即重载、禁用或退出时可能仍在后台运行的问题；现在会先安全取消并等待检测回调结束，再完成插件卸载，避免界面卡住或偶发崩溃。
- 提升配置和弹窗操作的稳定性：配置服务改为独立实现以减少更新时的重建范围，弹窗网格在高 DPI 下的点击区域由统一布局逻辑处理，降低边缘点击错位风险。
- 修复搜索按使用记录排序时，高频使用项目偶尔被排到后面的情况；现在会稳定优先显示名称匹配更准确且更常用、最近使用的结果。
- 优化弹窗调用外部文件选择的稳定性：关闭、重开或切换弹窗后，过期的选中文件结果会自动取消并丢弃，不会错误传给新命令。
- 优化弹窗图标刷新：连续刷新会合并为一次后台任务，隐藏窗口后会安全取消过期图标结果，减少无效磁盘读取和偶发图标错位。
- 大幅增强网址图标的自动获取：依次识别网页声明图标、多种常见图标地址并正确处理跳转和相对路径；遇受保护页面会查询公开图标索引，不提交网址路径或参数。PNG/ICO 保留透明通道，大面积纯色画布结果自动舍弃。
- 修复深色主题下“发光材质”和“玻璃材质”窗口边缘偶尔出现黑线的问题；统一高 DPI 圆角和折射边缘，中键弹窗圆角更平滑。
- 修复切换主题或背景参数时短暂跳回旧外观的问题；配置同步原子写入，其他窗口不会再读到旧状态。
- 修复暂停弹窗后中键仍被拦截的问题：暂停期间按键完整交还当前软件，连续快速触发自动合并减少卡顿。
- 强化钩子稳定性：超时不再强制终止线程，安全等待回收；宏录制在高频拖动时平滑更新，宏回放可被真实操作安全打断。
- 修复二级窗口失焦后阴影消失的问题；阴影持续保持并随窗口层级同步。
- 修复分类标签状态下键入字母或 IME 预选无法自动切换到搜索框的问题。
- 修复“捕获输出”混入进度信息和状态码的问题；成功时仅显示标准输出，失败才显示错误信息。
- 修复配置管理“生成诊断包”始终提示压缩失败的问题（LiteralPath 改为 Path）。
- 进一步收紧诊断与迁移保护：诊断包现在只包含版本、系统环境和汇总事件，不再附带原始日志、路径、网址、命令或输入内容；迁移 ZIP 会在解压前拦截不安全路径、链接和异常大小，恢复前自动创建保护备份，并在合并失败时不重载不完整配置。
- 修复同步文件夹暂时不存在时反复写入警告的问题；现在会按目录逐步延后重试，文件夹重新出现后自动恢复同步，不影响其他仍可用的同步目录。
- 修复随附插件和 SDK 示例的发布打包检查，并将实际打包纳入持续集成，减少发布时遗漏或构建脚本异常的风险。
- 优化图标刷新：双击空白处先显示占位图再后台读取；首次打开优先准备当前和相邻页面，其余延后创建。
- 优化弹窗重复唤出：背景未变化时复用已有缓存，减少等待和跳页。
- 优化弹窗首显与翻页流畅度：记录数据准备、背景、首帧等耗时，避免翻页动画逐帧强制同步绘制；网站图标获取改为复用受控后台队列，批量操作时更稳定。
- 优化后台任务退出流程：批量启动遇无响应及时停止，宏播放不会重叠；程序退出先停插件和后台再关窗口。
- 自动更新只接受单文件 EXE 发布包；下载检查完整性和取消状态，更新前再次确认文件有效。
- 修复 `{{config_dir}}` 指向错误位置的问题；`{{wan_ip}}` 弱网最多等 3 秒后回退，无效超时自动恢复默认。
- `/reload` 静默重载插件，仅轻量提示结果。
- 配置历史上限 10 条，超出自动删除最旧条目。
- 搜索逻辑从弹窗主类提取为独立服务，支持不依赖窗口和渲染的独立验证；搜索排序、本地快捷方式匹配、插件命令收集和反斜杠命令收集均可单独测试。
- 原生测试扩展至 465 行 20+ 场景，新增搜索多维度排序正确性、INI 损坏恢复（尾随反斜杠、未知转义、嵌入 NUL、非 ASCII 字符回环）、配置存储 UTF-8 损坏拒绝、迁移合并本地额外文件保留，以及命令分发更全面的边界检查。

### 体验优化

- 统一“添加宏”“添加批量启动”和“编辑系统图标”窗口排布与其他编辑窗口一致；高 DPI 或跨缩放屏幕时不再错位重叠。
- 优化中键弹窗滚轮翻页收尾：仅在肉眼不可察觉的距离内平稳停靠，减少拖顿感且避免突兀停顿。
- 精简清理缓存提示：完整显示清理范围、保留内容、已清理数量、释放空间和重试方式，不再截断关键信息。
- “导入 QuickLauncher”现完整兼容 data.json，保留分组/Dock/文件/网址/快捷键/命令/宏/批量启动关系；自定义图标正确复制到本地配置目录。
- 配置管理新增“清理缓存”：先保存配置、识别在用图标，再清除未使用图标、日志、诊断文件和历史；保留插件及其必要文件。
- 配置管理标注数据位置为“配置文件夹”并显示 APPDATA 路径；图标栏支持 Ctrl/Shift 多选。
- 降低了后台磁盘占用：配置与使用排序合并写入，文件夹同步改用变更通知而非轮询。
- 日志统一为 JSON Lines 格式，按时间/级别/模块记录；高频调试信息内存保留，常规日志批量写入并自动轮转。

## v0.5.1.8

### 功能新增

- 新增原生 DLL 插件系统基础能力，支持扫描用户插件目录、校验插件清单、安装和卸载插件、启用/禁用插件、查看权限摘要、记录插件错误，并在设置面板中查看插件状态。
- 新增受信任插件命令闭环，已启用插件可注册普通命令和 / 命令，并在弹窗搜索中展示和执行；Host API 支持按权限访问版本信息、日志、剪贴板、打开链接/文件和插件私有文件读写。
- 新增插件动态搜索源和独立 / 命令搜索体验，插件可在后台返回普通搜索结果，内置和插件 / 命令可带图标并在未设置图标时使用默认图标，普通图标搜索与 / 命令保持独立匹配。
- 新增插件声明式配置基础能力，支持读取 manifest 中的配置项，在设置页编辑插件配置，并通过 Host API 按权限读写插件私有配置文件，不写入主程序配置。
- 扩展插件 Host API，新增输入框、密码框、选项选择、确认提示、文件选择、通知提示、加载/进度反馈、结果回显、网络请求、进程执行和屏幕信息等能力，便于插件完成更完整的交互流程。
- 命令输出面板底部按钮改为四个等宽铺满窗口（刷新/复制/确定/更多），更多按钮可通过下拉菜单展示内置快捷操作和插件注册的扩展功能。
- 弹窗交互设置新增触发黑名单，默认内置 AutoCAD、SketchUp、Rhino、Blender、3ds Max、Maya 等常见 CAD/三维软件进程名，也可自行填写关键词或进程名；匹配时忽略大小写并支持模糊匹配，例如 `cad` 可匹配 `acad.exe`，在这些软件中使用中键或已设置的触发按键时，会保留按键原本功能而不打开弹窗。
- 优化触发黑名单编辑体验，软件较多时可在更宽敞的多行编辑框中逐行查看和维护，编辑窗口高度从 190px 增至 260px，方便查看更多条目；编辑窗口使用多行提示并支持中英文逗号/分号分隔；设置页底部仅显示是否已设置和条目数量，不再预览软件名称，避免长列表被截断。

### 修复
- 修复网址快捷方式编辑器在自动测试延迟或获取网站图标期间立即关闭窗口时，后台线程仍可能访问已关闭界面并引发偶发崩溃的问题；网络结果现在只会回到仍有效的编辑窗口，旧网址和重复请求的过期结果会自动丢弃。
- 修复资源管理器文件选择仍在捕获时关闭弹窗、命令输出面板关闭后继续接收旧任务输出、以及程序退出时插件命令仍在运行可能造成的跨线程回写和生命周期异常；退出现在会限时取消后台任务，慢插件不会让托盘退出无限卡住。
- 修复插件在后台命令中直接创建输入框、文件选择框、Toast 或消息框可能造成的跨线程 UI 不稳定，相关交互现统一交由主界面线程执行，单个回调异常也不会再中断后续回调。
- 修复配置面板图标网格在通过拖拽、菜单或对话框新增快捷方式时，新图标与末尾的“添加”图标重叠且“添加”图标未自动顺位向后移动的问题，优化后“添加”图标会在新增或删除快捷方式时平滑滑动到新末尾。
- 修复中键弹窗 `/` 搜索结果可能同时显示命令名和用法文本，导致看起来有两个图标名称的问题。
- 修复软件刚启动后立即唤出中键弹窗时，第一次显示可能先闪出窗口轮廓的问题，让弹窗在首帧内容准备好后再显示。
- 修复插件 / 命令在弹出输入框时可能因插件管理锁重入而卡死或触发未处理异常的问题，例如网络工具 `/port` 缺参数时的输入提示。
- 修复网络工具 `/ping` 可能在系统网络库中异常退出的问题，并提升 `/dns` 对异常解析结果的容错；同时修复 `/dns` 在输入参数后搜索结果可能消失或点击无效的问题，补齐网络插件的 Winsock 初始化流程。
- 修复网络工具 `/ping` 和 `/dns` 通过弹出输入框补全主机名后可能显示为空输出的问题，并提升插件命令多行输出的承载能力。
- 修复随附网络工具插件包重新生成时可能无法输出 `.wlplugin` 文件的问题。
- 修复插件命令后台执行期间关闭或切换命令输出面板时，旧后台任务可能继续回写窗口并导致异常退出的问题。
- 修复右键菜单、下拉菜单、托盘菜单、Toast 等弹出窗口关闭时阴影和窗口不同步的问题，减少关闭末尾的短暂闪烁并让阴影与窗口同步淡出；右键菜单、下拉菜单和托盘菜单在点击外部收起时会先关闭菜单，避免底层窗口先激活重绘造成瞬间闪烁。

### 体验优化

- 优化后台任务和日志性能：文件选择使用独立的高响应通道，普通网络与命令任务复用有界工作线程，命令输出可识别已重建的窗口实例；日志改为批量异步写入，并在主界面超过 2 秒无响应时记录卡顿线索，减少高频日志和线程创建对界面流畅度的影响。
- 统一弹窗出现和消失动画为轻量淡入淡出，减少中键弹窗和其他弹窗开合时的边框缩放重影，并支持通过设置统一调节动画时长或关闭动画。
- 文本、文件、时间和网络工具插件完善缺参数时的交互体验：文本类命令可弹输入框，文件命令可直接使用 WinLauncher 已捕获的资源管理器/桌面选中文件，倒计时和网络诊断命令可直接提示补全参数；所有内置插件已重新编译并重新生成安装包。
- 文件工具插件移除 `/copypath` 命令，仅保留 `/fileinfo` 和 `/filehash`，其中 `/filehash` 支持对选中的文件夹生成递归目录哈希。
- 网络工具 `/dns` 改为 DNS 泄露检测，可更快并发触发检测，并在命令面板中实时追加检测到的 DNS 解析服务器；结果包含服务器 IP 和国家，并用对齐表格显示检测结论。
- 网络工具 `/ping` 现在会在命令面板中实时追加每次请求的结果，不再等待全部 4 次请求结束后一次性显示。
- 命令输出面板现在会正确应用等宽字体显示结果，让 DNS 检测等表格型输出按 IP 位数稳定对齐。
- 插件管理页支持将 `.wlplugin` 插件包直接拖入页面安装，多个插件包可连续处理，并会提示安装结果。
- 内置和随附插件的 `/` 命令现在使用独立、压缩后的 `.ico` 图标：内置命令保留 `/settings`、`/reload`、`/about`，插件命令图标已换成更贴合功能含义的图形并随各自插件包提供；不再提供单独的 `/help` 命令。
- 命令输出面板底部新增刷新按钮，可保留上次执行时的参数和选中文件列表重新执行命令。
- 弹窗交互新增“选中时限”调节，可设置 0–20 秒；在 20 秒后继续增加可设为“无限”，此时只要中键弹窗持续显示，资源管理器或桌面中已选文件就不会因等待而失效，弹窗自动关闭时仍会安全清除。动画时长移至系统设置，并改为滑块调节后点击“应用”才生效。
- 优化命令面板刷新行为：刷新时清除旧输出内容，仅显示重新执行产生的新输出，不再拼接历史结果；修复长文本行在输出面板中被横向裁剪导致内容显示不完整的问题（如 SHA-256 哈希值尾部字符不可见）。
- 优化插件命令和插件 `/` 命令的执行反馈：点击后会立即打开输出面板并显示更顺滑的顺时针加载动画，后台执行完成后再展示结果，避免网络查询等慢命令看起来没有响应。

### 维护

- 增强异常崩溃诊断：崩溃时会在用户数据目录的 `crash` 文件夹保存小型转储和配套文本信息，记录版本、异常位置、线程及最近任务/回调轨迹，不收集完整进程内存且不会自动上传；同时新增原生稳定性测试，覆盖后台异常隔离、回调取消和崩溃产物生成。
- 完善项目维护规则、自动化测试和 CI 入口，明确后续维护以主程序稳定性为重心，优先保障启动、弹窗搜索、命令执行、自绘界面、配置、更新和发布链路；修复自动更新下载不完整时仍可能进入安装流程的问题，并让更新替换命令更安全地处理特殊路径字符；补齐主工程文件在 Visual Studio 工程视图中的同步检查，并增加更新伪装模式、版本资源一致性、静态回归测试、CI 自检和 GitHub Actions 检查，同时保留统一插件构建入口，降低发布包内容不一致的风险。
