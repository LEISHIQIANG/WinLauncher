# WinLauncher 更新记录

本文档用于记录 WinLauncher 每个版本的变化，内容面向用户和发布说明，不记录具体代码实现细节。

维护规则：

- 每次修改、修复、新增、体验调整、文档更新或发布准备，都必须同步更新本文档。
- 按版本分节记录，最新版本放在最上方。
- 同一个版本内不要重复记录相同内容；如果多个修改属于同一体验或同一能力，应合并成一条更清晰的发布说明。
- 优先使用用户能理解的介绍语言，例如“改进启动体验”“修复弹窗显示异常”“新增配置备份能力”，避免使用文件名、函数名、提交号等实现细节。
- 如果当前修改不改变产品功能，也要在对应版本的“文档与维护”或“稳定性与维护”中用简短语言记录。
- Codex、Antigravity、OpenCode、WorkBuddy 以及其他 AI 智能体在完成任何项目修改前，都必须检查并维护本文档。

## v0.5.1.7

### 体验优化

- 统一弹窗出现和消失动画为轻量淡入淡出，减少中键弹窗和其他弹窗开合时的边框缩放重影，并支持通过设置统一调节动画时长或关闭动画。

### 文档与维护

- 新增面向发布说明的版本更新记录，后续功能变化、修复和维护调整都将按版本集中记录。
- 明确智能体协作规则，要求 Codex、Antigravity/antygravity 等 AI 智能体在每次修改项目时同步维护更新记录，并避免同版本内容重复。
- 完善插件系统优化计划，明确插件能力边界、运行时目录、安全策略、阶段验收和后续 SDK/隔离进程路线。
- 新增 / 命令（Slash Commands）系统设计，包括概念定义、内置命令规划、插件注册方式、搜索匹配逻辑和执行路径，与普通搜索保持两条独立匹配路径。
