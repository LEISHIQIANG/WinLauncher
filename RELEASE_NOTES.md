# WinLauncher 更新记录

本文档面向用户和发布说明，只记录产品功能变化（新增、修复、体验优化），不记录项目文档、开发计划等内部维护内容。

维护规则：

- 每次功能修改、修复或体验调整都必须同步更新本文档。
- 按版本分节记录，最新版本放在最上方。
- 同一个版本内不要出现自相矛盾的内容（如新增又删除同一功能），应直接合并为最终状态。
- 同版本内多个相关修改应合并为一条清晰的发布说明。
- 优先使用用户能理解的介绍语言，避免文件名、函数名、提交号等实现细节。
- Codex、Antigravity、OpenCode、WorkBuddy 以及其他 AI 智能体在完成任何项目修改前，都必须检查并维护本文档。

## v0.5.1.9

### 功能新增

- 编辑命令时，类型菜单会根据本机已检测到的运行环境显示 Python 和 Git Bash；未安装或不可用的解释器不会再出现在可选项中，避免创建后才发现无法运行。首次检测尚未完成时会明确提示正在检测；已有命令的类型与内容保持不变，运行失败时仍会给出安装或配置建议。
- 所有内置预设现均配有编译进 WinLauncher 的默认图标：系统工具与常用命令会在未指定自定义图标时直接显示对应图标，网站预设则采用各官网提供的图标。Windows 原本提供图标的项目直接保留系统原图，没有独立系统图标的项目才使用内置绘制图标；用户仍可为任一预设改用自己的图标，清空后会自动恢复对应的内置默认图标。图标资源移除了重复的旧格式帧，以减小程序体积并保留高 DPI 显示所需尺寸。
- 内置图标的“系统”分类新增磁盘管理、服务、事件查看器、资源监视器和系统信息，可一键添加到启动器，方便快速处理磁盘、服务、性能与系统诊断工作。
- 新增本地诊断与迁移工具：可从配置管理生成脱敏诊断 ZIP，或导出/导入包含启动器配置、历史、插件状态与插件私有数据的迁移备份；两项操作均会弹出系统"另存为"对话框，让用户选择文件保存路径，默认定位到桌面并预填带时间戳的文件名；备份不携带插件程序、日志或崩溃转储，恢复前会校验清单并保留当前数据的自动保护副本。
- 搜索结果现会优先保留名称匹配质量，再结合本机成功使用次数和最近使用时间排序；使用记录仅保存在本机，不记录搜索内容、文件路径、命令参数或输出，并可随时在配置管理中清除。

- 弹窗外观新增“图标字号”调节项，可在 8–32 px 之间调整弹窗中图标名称的文字大小；设置会自动保存，并保留搜索框原有的自适应字号。
- 弹窗外观新增“标题大小”1–9 档位：以第 5 档为默认，标题栏和搜索框仅作细微同步缩放，同时调整分类文字、圆角矩形的选中指示与悬停预选样式、以及提示图标比例；外观参数按整体、网格、文字与停靠区的顺序重新整理，便于快速找到需要的调节项。
- 完善设置中的“关于软件”页面：现在会介绍快捷启动、搜索分类、命令自动化、外观扩展、本地数据与诊断能力，并展示版本和运行方式；长说明会按清晰分行完整显示，底部仅保留可点击的 GitHub 开源地址，同时将“插件管理”前置到“配置管理”之前，便于优先管理扩展能力。

### 修复

- 大幅增强网址图标的自动获取：现在会依次识别网页声明图标、网站应用清单、品牌分享图和多种常见图标地址，并正确处理网页跳转及相对图标路径；遇到人机验证、浏览器访问保护或网站未公开图标等情况时，会按网址主机名及主域名依次查询多个公开图标索引，不会提交你填写的网址路径、参数或登录信息。获取到的 PNG、ICO 等原始格式会保留透明通道，启动器不会额外添加黑色或白色底图；公共回退中带大面积纯白或纯黑画布的结果会自动舍弃。DeepSeek 平台等受保护页面及更多非标准网站现在也可自动设置图标。
- 修复深色主题下“发光材质”和“玻璃材质”窗口边缘偶尔出现细黑线或整圈黑色描边的问题；设置窗口、弹窗等共享窗口现在使用统一的高 DPI 圆角和细微折射边缘，中键弹窗四个圆角也会更干净、平滑。
- 修复切换深浅主题、窗口材质或背景效果参数时可能短暂跳回旧外观的问题；所有配置现在均会同步、原子地写入磁盘，其他窗口读取时不会再拿到旧状态；仅运行日志继续采用批量写入以控制磁盘占用。
- 修复清理本地崩溃诊断文件时可能导致程序无法完成构建的问题；诊断文件仍会按数量和总大小自动清理，避免长期占用过多磁盘空间。
- 修复暂停弹窗后中键或侧键仍可能被启动器拦截的问题：暂停期间触发按键会完整交还给当前软件，双击 Alt 仅用于恢复弹窗且不会阻断正常键盘输入；连续快速触发会自动合并等待中的请求，减少卡顿和重复闪烁。
- 强化鼠标、键盘与宏钩子的稳定性：钩子关闭超时时不再强制终止线程，而是安全等待后续回收，避免异常退出或重复安装；宏录制在高频拖动时会平滑更新预览，宏回放可被真实键鼠操作立即安全打断，并自动释放已按下的按键和鼠标按钮，减少卡键或卡拖拽风险。
- 修复添加/编辑快捷方式、确认提示、文本输入和命令输出等二级窗口在失去焦点后阴影可能消失的问题；窗口只要仍显示，阴影会持续保持并随窗口层级同步，无需重新点击窗口恢复。
- 修复弹窗处于分类标签状态时，直接键入字母或通过输入法键入汉字（预选阶段）无法自动切换到搜索框的问题；现在只要开始输入，搜索框即立即显示并接收内容，该切换不保存状态，下次打开弹窗仍显示分类栏。

- 修复自定义命令“捕获输出”会将 `curl` 等工具写入错误流的进度信息混入正文、并在成功结果前后附加运行状态和退出码的问题；成功时面板现在只显示命令的实际标准输出，命令失败时才显示必要的错误信息。
- 修复配置管理"生成诊断包"始终提示"压缩失败"的问题；原因是调用 PowerShell `Compress-Archive` 时误用了不支持通配符的 `-LiteralPath`，改为 `-Path` 后通配符可正确展开，诊断 ZIP 能正常生成。
- 优化快捷方式图标刷新：双击弹窗空白处后，当前可见图标会先短暂显示占位图，再在后台重新读取完成后替换为最新图标，保留明确的刷新反馈同时避免快捷方式较多时界面卡顿；首次打开优先准备当前页面和相邻页面，其余页面按浏览需要延后创建，减少首次唤出引起的短暂停顿；关闭、重建或再次刷新弹窗时，旧图标结果会自动丢弃。
- 优化弹窗重复唤出体验：背景未变化时会复用已生成的背景缓存，不再每次都重新捕获和合成；场景筛选开启时也会正确保留当前可见分类，减少重复唤出时的等待和跳页。
- 优化批量启动、宏播放、鼠标触发、键盘快捷键和宏录制的后台任务退出流程：通常会更快、更稳定地完成关闭；批量启动遇到无响应界面时会及时停止等待，宏播放未完全退出时不会与新的播放操作重叠；程序退出会先停止插件、界面回调、更新检查/下载和后台任务再关闭窗口。弹窗在资源管理器或桌面文件选择仍在读取时被关闭、销毁或替换后，会及时停止旧查询；命令输出面板在关闭、切换结果或刷新时会取消旧任务并丢弃旧输出，后台任务队列繁忙时也会立即停止加载提示并给出明确反馈，避免界面持续转圈、旧内容回写或看似无响应。
- 自动更新现在只接受可直接替换的单文件 EXE 发布包；下载会检查服务器响应、写入完整性和取消状态，失败文件会自动清理。更新替换前会再次确认下载文件有效，避免更新失败时意外退出；手动重启或重启钩子失败时也会给出明确提示，便于及时重试。
- 修复自定义命令中的 `{{config_dir}}` 指向错误位置的问题，现在会返回当前用户真实的 WinLauncher 配置目录；`{{wan_ip}}` 查询在弱网时最多等待 3 秒后安全回退，命令的无效超时配置也会自动恢复为安全默认值，并明确提示命令因超时被终止。
- 内置 `/reload` 现在会静默重新扫描和加载插件，仅以轻量提示反馈结果，不再打开无实际输出的命令面板。
- 配置历史上限调整为 10 条；程序启动及每次生成新备份后，超出数量的最旧条目会自动删除，存量超出的历史也会在下次启动时立即清理，避免历史目录长期积累大量备份文件。

### 体验优化

- 完善长期维护规范与架构说明：后台任务、界面生命周期、配置保存、插件兼容、可选命令环境和代码清理现有统一边界与验证要求，后续维护更容易保持稳定一致。
- 配置管理移除了“打开配置文件”，并在“清除使用记录”后新增“清理缓存”：会先保存当前配置、识别仍在使用的图标，再清除未使用图标、日志、崩溃与诊断文件、配置历史和临时缓存；清理期间会显示等待动画，完成后会报告删除数量和释放空间。已安装插件及其必要文件会保留，避免影响当前功能。
- 配置管理现将本机数据位置标注为“配置文件夹”，并显示、打开 `%APPDATA%\WinLauncher`（例如 `C:\Users\Administrator\AppData\Roaming\WinLauncher`），便于统一查看配置、日志、备份和插件数据。
- 配置窗口的图标栏现支持 Ctrl/Shift 多选；右键点击已选项目会保留当前多选状态，多选菜单仅提供“删除”和“获取图标”，避免误编辑单个项目。“获取图标”会自动忽略非网址项目，并通过后台并行获取所选网站图标后一次性应用结果。
- 优化“配置管理”的操作顺序：查看文件与日志、备份和回滚、迁移和导入、诊断与记录清理会按用途连续排列；清除配置和历史保留在最后一行，便于在执行高风险操作前先完成保护或恢复。
- 降低了启动器的后台磁盘占用：设置与本机使用排序会合并后再安全写入，文件夹同步改为按系统文件变更通知更新，不再持续轮询扫描；配置、使用记录和插件状态均会在退出及明确的数据操作前可靠保存。
- 日志现统一保存为可检索的 JSON Lines 文件，按时间、级别、任务、模块和事件码记录；高频调试信息改为内存保留，常规日志批量写入并自动轮转、清理。诊断包会附带最新的脱敏结构化事件，便于定位问题且避免日志长期膨胀。

## v0.5.1.8

### 功能新增

- 新增原生 DLL 插件系统基础能力，支持扫描用户插件目录、校验插件清单、安装和卸载插件、启用/禁用插件、查看权限摘要、记录插件错误，并在设置面板中查看插件状态。
- 新增受信任插件命令闭环，已启用插件可注册普通命令和 / 命令，并在弹窗搜索中展示和执行；Host API 支持按权限访问版本信息、日志、剪贴板、打开链接/文件和插件私有文件读写。
- 新增插件动态搜索源和独立 / 命令搜索体验，插件可在后台返回普通搜索结果，内置和插件 / 命令可带图标并在未设置图标时使用默认图标，普通图标搜索与 / 命令保持独立匹配。
- 新增插件声明式配置基础能力，支持读取 manifest 中的配置项，在设置页编辑插件配置，并通过 Host API 按权限读写插件私有配置文件，不写入主程序配置。
- 扩展插件 Host API，新增输入框、密码框、选项选择、确认提示、文件选择、通知提示、加载/进度反馈、结果回显、网络请求、进程执行和屏幕信息等能力，便于插件完成更完整的交互流程。
- 命令输出面板底部按钮改为四个等宽铺满窗口（刷新/复制/确定/更多），更多按钮可通过下拉菜单展示内置快捷操作和插件注册的扩展功能。
- 弹窗交互设置新增触发黑名单，默认内置 AutoCAD、SketchUp、Rhino、Blender、3ds Max、Maya 等常见 CAD/三维软件进程名，也可自行填写关键词或进程名；匹配时忽略大小写并支持模糊匹配，例如 `cad` 可匹配 `acad.exe`，在这些软件中使用中键或已设置的触发按键时，会保留按键原本功能而不打开弹窗。
- 优化触发黑名单编辑体验，软件较多时可在更宽敞的多行编辑框中逐行查看和维护，编辑窗口高度从 190px 增至 260px，方便查看更多条目；编辑窗口使用多行提示并支持中英文逗号/分号分隔；设置页底部仅显示是否已设置和条目数量，不再预览软件名称，避免长列表被截断。

### 修复
- 修复网址快捷方式编辑器在自动测试延迟或获取网站图标期间立即关闭窗口时，后台线程仍可能访问已关闭界面并引发偶发崩溃的问题；网络结果现在只会回到仍有效的编辑窗口，旧网址和重复请求的过期结果会自动丢弃。
- 修复资源管理器文件选择仍在捕获时关闭弹窗、命令输出面板关闭后继续接收旧任务输出、以及程序退出时插件命令仍在运行可能造成的跨线程回写和生命周期异常；退出现在会限时取消后台任务，慢插件不会让托盘退出无限卡住。
- 修复插件在后台命令中直接创建输入框、文件选择框、Toast 或消息框可能造成的跨线程 UI 不稳定，相关交互现统一交由主界面线程执行，单个回调异常也不会再中断后续回调。
- 修复配置面板图标网格在通过拖拽、菜单或对话框新增快捷方式时，新图标与末尾的“添加”图标重叠且“添加”图标未自动顺位向后移动的问题，优化后“添加”图标会在新增或删除快捷方式时平滑滑动到新末尾。
- 修复中键弹窗 `/` 搜索结果可能同时显示命令名和用法文本，导致看起来有两个图标名称的问题。
- 修复软件刚启动后立即唤出中键弹窗时，第一次显示可能先闪出窗口轮廓的问题，让弹窗在首帧内容准备好后再显示。
- 修复插件 / 命令在弹出输入框时可能因插件管理锁重入而卡死或触发未处理异常的问题，例如网络工具 `/port` 缺参数时的输入提示。
- 修复网络工具 `/ping` 可能在系统网络库中异常退出的问题，并提升 `/dns` 对异常解析结果的容错；同时修复 `/dns` 在输入参数后搜索结果可能消失或点击无效的问题，补齐网络插件的 Winsock 初始化流程。
- 修复网络工具 `/ping` 和 `/dns` 通过弹出输入框补全主机名后可能显示为空输出的问题，并提升插件命令多行输出的承载能力。
- 修复随附网络工具插件包重新生成时可能无法输出 `.wlplugin` 文件的问题。
- 修复插件命令后台执行期间关闭或切换命令输出面板时，旧后台任务可能继续回写窗口并导致异常退出的问题。
- 修复右键菜单、下拉菜单、托盘菜单、Toast 等弹出窗口关闭时阴影和窗口不同步的问题，减少关闭末尾的短暂闪烁并让阴影与窗口同步淡出；右键菜单、下拉菜单和托盘菜单在点击外部收起时会先关闭菜单，避免底层窗口先激活重绘造成瞬间闪烁。

### 体验优化

- 优化后台任务和日志性能：文件选择使用独立的高响应通道，普通网络与命令任务复用有界工作线程，命令输出可识别已重建的窗口实例；日志改为批量异步写入，并在主界面超过 2 秒无响应时记录卡顿线索，减少高频日志和线程创建对界面流畅度的影响。
- 统一弹窗出现和消失动画为轻量淡入淡出，减少中键弹窗和其他弹窗开合时的边框缩放重影，并支持通过设置统一调节动画时长或关闭动画。
- 文本、文件、时间和网络工具插件完善缺参数时的交互体验：文本类命令可弹输入框，文件命令可直接使用 WinLauncher 已捕获的资源管理器/桌面选中文件，倒计时和网络诊断命令可直接提示补全参数；所有内置插件已重新编译并重新生成安装包。
- 文件工具插件移除 `/copypath` 命令，仅保留 `/fileinfo` 和 `/filehash`，其中 `/filehash` 支持对选中的文件夹生成递归目录哈希。
- 网络工具 `/dns` 改为 DNS 泄露检测，可更快并发触发检测，并在命令面板中实时追加检测到的 DNS 解析服务器；结果包含服务器 IP 和国家，并用对齐表格显示检测结论。
- 网络工具 `/ping` 现在会在命令面板中实时追加每次请求的结果，不再等待全部 4 次请求结束后一次性显示。
- 命令输出面板现在会正确应用等宽字体显示结果，让 DNS 检测等表格型输出按 IP 位数稳定对齐。
- 插件管理页支持将 `.wlplugin` 插件包直接拖入页面安装，多个插件包可连续处理，并会提示安装结果。
- 内置和随附插件的 `/` 命令现在使用独立、压缩后的 `.ico` 图标：内置命令保留 `/settings`、`/reload`、`/about`，插件命令图标已换成更贴合功能含义的图形并随各自插件包提供；不再提供单独的 `/help` 命令。
- 命令输出面板底部新增刷新按钮，可保留上次执行时的参数和选中文件列表重新执行命令。
- 弹窗交互新增“选中时限”调节，可设置 0–20 秒；在 20 秒后继续增加可设为“无限”，此时只要中键弹窗持续显示，资源管理器或桌面中已选文件就不会因等待而失效，弹窗自动关闭时仍会安全清除。动画时长移至系统设置，并改为滑块调节后点击“应用”才生效。
- 优化命令面板刷新行为：刷新时清除旧输出内容，仅显示重新执行产生的新输出，不再拼接历史结果；修复长文本行在输出面板中被横向裁剪导致内容显示不完整的问题（如 SHA-256 哈希值尾部字符不可见）。
- 优化插件命令和插件 `/` 命令的执行反馈：点击后会立即打开输出面板并显示更顺滑的顺时针加载动画，后台执行完成后再展示结果，避免网络查询等慢命令看起来没有响应。

### 维护

- 增强异常崩溃诊断：崩溃时会在用户数据目录的 `crash` 文件夹保存小型转储和配套文本信息，记录版本、异常位置、线程及最近任务/回调轨迹，不收集完整进程内存且不会自动上传；同时新增原生稳定性测试，覆盖后台异常隔离、回调取消和崩溃产物生成。
- 完善项目维护规则、自动化测试和 CI 入口，明确后续维护以主程序稳定性为重心，优先保障启动、弹窗搜索、命令执行、自绘界面、配置、更新和发布链路；修复自动更新下载不完整时仍可能进入安装流程的问题，并让更新替换命令更安全地处理特殊路径字符；补齐主工程文件在 Visual Studio 工程视图中的同步检查，并增加更新伪装模式、版本资源一致性、静态回归测试、CI 自检和 GitHub Actions 检查，同时保留统一插件构建入口，降低发布包内容不一致的风险。
