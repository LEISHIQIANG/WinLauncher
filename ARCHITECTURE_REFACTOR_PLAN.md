# WinLauncher 保真架构调整计划书

> 状态：规划文档。本文不授权或包含任何源代码、配置、工程文件、脚本或发布版本变更。
>
> 目标：在未来的实施中提升内部可维护性、依赖清晰度、可测试性和死代码治理能力，同时确保用户能够观察到的功能、数据和自绘 UI **完全不变**。

## 1. 最高约束与完成定义

本项目是原生 Win32/Direct2D 启动器。架构整理只能改变代码的归属和依赖关系，不能改变产品的任何可观察结果。

### 1.1 不可变契约

- 不改变窗口类、创建顺序、窗口层级、置顶逻辑、阴影、圆角、透明度、动画时长、动画曲线、绘制顺序、DPI 计算、字体、颜色、图标、文本、控件尺寸或命中区域。
- 不改变启动、单实例、托盘、鼠标/键盘触发、双 Alt、暂停、Popup 搜索、分类、场景、快捷方式、命令、宏、批量启动、更新、诊断、迁移备份和退出的行为与时序。
- 不改变 `%APPDATA%\WinLauncher` 的路径、INI/JSON 键名和值、写入顺序、转义规则、默认值、历史/备份规则、缓存位置、日志格式、插件私有数据和已有用户数据的兼容性。
- 不改变 `WLHostApiV1` 的二进制布局、字段含义、回调时机、权限、插件 manifest、SDK 头文件、SDK 模板、样例或内置插件命令行为。
- 不新增用户可见功能、设置、按钮、提示、日志字段、配置 schema 或数据迁移；不升级版本号。
- 不以格式化、抽象或“看起来未使用”为由改变异常路径、超时、取消、重试、资源释放、线程退出或错误文字。

### 1.2 死代码的严格定义

一个实现、声明、资源或工程条目只有同时满足以下条件时才可删除：

1. 没有生产调用、消息入口、窗口过程入口、回调注册、虚函数/导出表入口或动态加载入口。
2. 没有配置/序列化字段、旧数据解析、命令名称、快捷方式类型或资源 ID 依赖。
3. 没有 `WLHostApiV1`、插件 DLL、manifest、SDK 头文件、模板或样例消费者。
4. 没有 MSBuild、filters、打包脚本、CI、测试、安装/更新流程或文档引用。
5. 删除后通过全部静态、原生、包内容、行为基线和人工 UI 验收。

不能证明时必须保留，并把候选项登记为“待审计”，不能使用 `#if 0`、注释掉实现或未引用声明来伪装删除。

## 2. 当前结构盘点

当前主工程约含 66 个实现文件和 93 个头文件，主要由以下区域组成：

| 区域 | 当前职责 | 未来整理边界 |
|---|---|---|
| `WinLauncher/App` | `Application`、`AppContext`、后台任务、UI 调度、日志、崩溃、插件加载与状态。 | 保持唯一组合根和既有关闭顺序；移除对具体 UI 的不必要编译期依赖。 |
| 根目录窗口代码 | Popup、玻璃/阴影、Tray、Toast、鼠标/键盘钩子、快捷方式管理。 | 保持窗口、绘制和消息行为；将纯业务/编码/路径工具下沉至服务。 |
| `WinLauncher/Config` | 配置主窗口、设置、快捷方式页、菜单、命令面板、各类编辑对话框及自绘控件。 | 保持所有像素和交互；抽取重复状态转换、资源释放和服务请求。 |
| `WinLauncher/Services` | 配置、图标、环境、命令变量、更新、文件选择、宏、同步、备份、诊断。 | 服务不得依赖具体 Config/Popup 窗口；保留 Win32 适配与数据格式。 |
| `Model`、`ViewModel`、`UI` | 值对象、展示状态、渲染层和控件。 | 只承载模型、视图状态和绘制；不构造持久化/网络/插件服务。 |
| `plugins`、`SDK` | 五个内置插件、包脚本、ABI、模板、样例与开发文档。 | 维持 ABI 与包内容；仅消除经证明的内部重复。 |
| `scripts`、`tests`、`.github` | 维护门禁、CI、静态检查、场景回归和原生稳定性测试。 | 把本计划的架构、格式、审计和保真检查纳入既有入口。 |

### 2.1 已识别的优先审计热点

- `PopupWindow.cpp` 同时处理搜索、命令变量、脚本创建、Shell 启动、输出读取/解码、超时、命令面板和窗口呈现。未来只允许提取纯执行能力，Popup 的输入、消息、显示和刷新时间点必须保持。
- `SettingsPage.cpp`、`ConfigWindow.cpp`、`ShortcutPage.cpp` 负责页面绘制、命中、窗口协调和数据提交。未来先抽取无 UI 的数据映射和重复结果应用逻辑，再由原页面调用。
- `IniConfigRepository.h` 承担接口、解析、写入、备份和历史处理。未来实施文件拆分时，必须对配置输入输出做字节/字段回归。
- `UIStyle.h` 含可变全局外观状态；多个菜单、对话框、Toast 和命令面板拥有静态实例或缓存。未来替换为应用持有的状态或窗口协调对象，但不改变单实例和缓存表现。
- `CommandPanelWindow.cpp` 仍定义本地自定义窗口消息。未来将消息常量登记到 `AppMessages.h`，数字、payload 所有权和 generation 过滤逻辑保持不变。
- `CommandVariableService` 与插件管理存在具体 UI 依赖。未来通过中性接口请求交互，实际仍由当前 `PromptWindow`、确认框、文件选择与 Toast 完成。

## 3. 目标架构

目标架构仅用于约束内部依赖，**不是** 新建第二套 UI 或服务定位器：

```text
Application（唯一组合根：创建、启动、关闭顺序）
        │
        ├── AppContext（长期服务、生命周期令牌、窗口协调）
        │
        ├── Contracts（纯接口、请求、结果与快照）
        │       ├── IConfigService / IIconService
        │       ├── ICommandExecutionService
        │       ├── IUserInteractionService
        │       └── IWindowCoordinator
        │
        ├── Services（配置、图标、命令、更新、选择、宏、同步）
        ├── Model / ViewModel（无具体窗口依赖）
        └── UI（现有自绘控件、渲染层、Popup、Config 与对话框）
```

### 3.1 依赖规则

- `Application` 是唯一创建具体服务、绑定 `UiDispatcher`、安装钩子和决定 shutdown 顺序的位置。
- `AppContext` 可持有长期服务，不得演变为任意位置可查询的全局服务定位器。
- Model 和请求/结果值对象不包含 `HWND`、Direct2D 资源或 Config 窗口引用。
- Services 可依赖模型、Contracts 和必要 Win32 平台 API，但不得包含具体 Popup、Config、菜单、Toast、对话框或自绘控件头文件。
- UI 不直接构造配置、网络、插件、后台服务；通过已有上下文或抽象接口提交请求、接收快照并在 UI 线程显示。
- 所有 Direct2D、HWND、菜单、Toast、对话框和窗口状态修改继续在 UI 线程发生。
- 所有后台工作继续通过 `BackgroundTaskService`；系统钩子/watcher 的必要受控线程保留其停止事件、超时回收与析构保护。

## 4. 分模块实施蓝图

以下顺序用于未来实际实施；每一节都是独立可构建、可验证、可回退的内部检查点，但不代表向用户分阶段发布。

### 4.1 阶段 A：行为基线与逐文件审计

1. 为全部生产源码、头文件、插件、SDK、脚本、测试、CI、项目和 filters 建立审计清单。
2. 每个文件只能标为“保留原样”“仅格式化”“仅移除无效依赖”“内部拆分”“接口迁移”“候选删除”之一，并说明理由。
3. 固化配置读写、命令输出、插件行为、消息顺序、窗口创建/销毁、后台任务、取消和日志事件的基线夹具。
4. 在固定主题、字体、DPI、窗口尺寸和测试数据下截取自绘 UI 基线图。
5. 任何后续改动必须首先关联到一个基线场景和至少一个自动化检查。

### 4.2 阶段 B：应用根、消息与生命周期

- 保持 `Application::Run`、单实例检测、主窗口创建、托盘、钩子、消息循环和 `Shutdown` 的外部顺序不变。
- 把服务创建、窗口回调注册和销毁清理明确收拢至组合根；现有顺序“取消插件/异步工作 → 停止 UI 投递 → 关闭窗口 → 释放服务”不得改变。
- 将所有 `WM_APP` 业务消息登记到 `AppMessages.h`。对命令面板消息，保留现有消息数值、投递条件、generation 编号、过期丢弃规则和 `PostMessageW` 失败处理。
- 为消息 payload 定义唯一所有权：发送方创建、接收方接管、投递失败立即释放、窗口销毁后不再访问。不得改变 payload 内容或通知时机。
- 以受控 `IWindowCoordinator` 收敛窗口静态实例、静态上下文和 closing 集合；外部可见的单实例、菜单互斥、重入和关闭动画必须完全一致。

### 4.3 阶段 C：Popup 搜索与命令执行

- 定义内部 `CommandExecutionRequest`：命令类型、原始命令、参数、选中文件快照、Shell 环境、超时、最大输出、刷新标识和取消令牌。
- 定义内部 `CommandExecutionResult`：标准输出、错误输出、退出码、超时/截断状态和流式片段。字段只表达现有能力，不新增用户能力。
- 由 `ICommandExecutionService` 承接变量展开、脚本文件创建、参数转义、编码解码、进程等待、超时、输出截断和结果格式化。
- Popup 继续在原消息路径决定何时展示命令面板、何时显示加载状态、何时刷新、何时取消；命令面板继续负责复制、刷新、更多菜单和绘制。
- `EnvironmentDetector` 是可选解释器/终端可用性的唯一来源。清理 Popup 内部重复查找前，必须比对成功路径、缺失路径、错误提示和已有已保存命令的执行结果。
- 搜索排序、分类、场景、图标分页、背景缓存、文件选区捕获和关闭取消不改变算法或调用点。

### 4.4 阶段 D：配置、快捷方式与持久化

- 将 `IniConfigRepository` 分为公共接口、实现文件和私有序列化/备份帮助单元；不得修改 INI 键名、字段顺序、转义、默认值、历史文件命名、备份数量、写入路径或原子保存方式。
- 将 `ShortcutManager` 内部的配置访问、系统路径解析、图标解析和运行时操作拆开；提供行为完全等价的兼容门面，直到所有调用方迁移完成。
- `ConfigWindow`、`SettingsPage`、`ShortcutPage` 和编辑窗通过视图状态与服务请求协作，不直接写入其他窗口私有状态。
- 保存始终遵守“先成功落盘，再通知观察者”；Popup、设置页、插件状态和外观刷新必须保持当前时序。
- 本计划禁止新增 config schema、迁移旧数据或清理任何用户字段；任何历史解析分支在没有完整数据夹具前不得移除。

### 4.5 阶段 E：自绘 UI、页面和表单

- `GlassWindow`、`ShadowWindow`、`PopupWindow`、`ToastWindow`、Tray 菜单、右键菜单、下拉菜单、命令面板和所有 Config 对话框保留现有窗口类、绘制顺序与动画参数。
- 将 `UIStyle` 的可变状态逐步收敛为内部 `ThemeSnapshot`。快照产生、应用和重新绘制必须沿用当前保存/通知路径，并返回相同颜色、透明度、字体与 DPI 数值。
- 抽取 `SettingsPage`、`ShortcutPage` 和编辑表单中可证明重复的命中测试、资源释放、编辑结果比较和控件状态转换；不得调整矩形计算、焦点顺序、键盘导航、按钮文字或任何点击区域。
- 画刷、位图与图标缓存从裸全局迁至窗口实例或受控共享缓存时，缓存失效条件、首帧准备、DPI 重建、阴影保持、失焦和关闭淡出行为不得变化。
- 所有 COM、`Release`、`DestroyIcon` 整理必须通过多次打开/关闭、DPI 切换、主题切换和截图回归验证，以避免黑边、闪烁、首帧空白或资源重用退化。

### 4.6 阶段 F：服务、输入、异步和系统集成

- 为命令变量和插件交互引入 `IUserInteractionService`；其实现仍调用现有 Prompt、确认、选择和 Toast 窗口，调用线程、同步/异步语义和显示文案不变。
- `BackgroundTaskService`、`UiDispatcher`、`CallbackGuard`、`CrashReporter` 保持职责不变；只统一 generation、取消令牌和窗口存活判断的内部表达。
- 鼠标钩子、键盘钩子、宏录制、宏播放、批量启动、文件夹 watcher、使用历史保存和更新服务保留当前线程模型、定时参数、错误处理和退出行为。
- 所有绕开 `BackgroundTaskService` 的线程必须列为白名单，并具备同等停止事件、超时等待和延迟回收逻辑；无法证明必要性的实现才进入删除候选。
- 诊断、备份、更新、图标抓取和文件选择继续遵守现有隐私边界、路径安全和取消语义。

### 4.7 阶段 G：插件、SDK、工程与脚本

- `PluginManager` 保持加载、权限、启停、命令、动态搜索、私有配置、Host API 和关闭等待行为，只通过 Contracts 请求 UI。
- `WLHostApiV1` 继续 append-only；本计划不新增字段、更改大小、修改导出、调整 callback 或改变 manifest。
- 提取插件共享代码前，逐一回归五个内置插件的命令名称、参数、图标、权限摘要、输出、错误文本、文件上下文和 `.wlplugin` 包内容。
- 新增/移动/删除主工程文件必须同时更新 `.vcxproj` 和 `.vcxproj.filters`；插件、SDK 样例和原生测试工程维持相同完整性。
- 新增根级 `.editorconfig` 与 `.clang-format` 时，格式检查只读运行。格式化不得触及资源、生成物、用户数据、插件包、字符串内容或预处理条件。

## 5. 保真验证矩阵

### 5.1 像素与交互验证

- 固定 Windows 版本、字体、主题、100%/125%/150% DPI、窗口尺寸和测试数据，捕获启动、托盘、Popup、Config、Settings、Shortcut 编辑、命令面板、菜单、Toast、插件、诊断和更新页面。
- 时钟、网络、系统图标和阴影等动态区域通过确定性替身或局部遮罩处理；其余区域必须零像素差异。
- 每个场景同时记录窗口类、可见性、位置、大小、Z 序、焦点、命中结果、消息序列、后台任务名、取消时机和最终数据。
- 所有配置/插件/SDK 输入输出使用字段级或字节级比较，保证外部格式不变。

### 5.2 必过功能场景

- 启动、单实例、托盘、暂停/恢复、鼠标触发、键盘触发、双 Alt、窗口关闭和干净退出。
- 本地搜索、分类、场景、使用记录排序、图标懒加载/刷新、背景缓存、资源管理器/桌面选区和重复唤出。
- 普通、网址、系统图标、热键、命令、宏、批量启动快捷方式的新增、编辑、删除、图标与备份/恢复。
- CMD、PowerShell、Python、Git Bash、变量、超时、输出捕获、流式输出、刷新、取消与失败提示。
- 所有设置分类、主题、材质、缩放、动画、阴影、焦点、右键/下拉菜单外部点击关闭。
- 插件缺失、无效、禁用、启用、安装、卸载、动态搜索、普通/斜杠命令、私有配置、文件上下文和宿主交互。
- 诊断、迁移备份、更新检查/下载失败、日志、崩溃诊断、文件夹同步、宏中断和批量启动取消。

## 6. 强制门禁

- 保留现有 `scripts/maintenance_check.ps1`、`tests/run_tests.ps1`、场景回归、`scripts/ci_check.ps1`、Release x64 构建、原生稳定性测试及插件/SDK 打包。
- 增加架构静态检查：Services 不得依赖具体 UI；自定义应用消息不得绕开 `AppMessages.h`；组合根唯一；静态跨窗口状态受控；后台线程白名单；工程与 filters 同步。
- 增加死代码审计：每个删除项必须附带五项证据，审计记录必须由测试验证。
- 增加格式门禁：统一格式配置、只读校验和明确差异报告。
- 每个未来检查点必须通过：格式、架构、静态测试、相关原生测试、UI 基线、数据夹具、`git diff --check` 和维护检查；任一失败立即回退该检查点。

## 7. 发布与文档规则

- 本计划本身不改变产品版本；未来内部重构完成时继续使用当时 `WinLauncher/version.h` 的当前版本并更新对应 `RELEASE_NOTES.md` 节。
- 发布说明只描述用户可感知的稳定性维护，不将内部文件拆分、接口名称或格式化作为产品功能宣传。
- `MAINTENANCE.md`、README、测试说明和本计划必须与最终实际边界一致；任何未实施条目不得被写成已完成。
